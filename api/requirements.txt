# Vercel serverless runtime deps only, pinned to match requirements.txt -
# no uvicorn (Vercel provides its own ASGI invocation) and no test deps.
fastapi==0.139.2
pydantic==2.13.4
anthropic==0.117.0
