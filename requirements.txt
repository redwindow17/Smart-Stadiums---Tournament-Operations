# Runtime + test dependencies, pinned for reproducible, auditable installs
# (`pip-audit -r requirements.txt` runs in CI against exactly these versions).

# Runtime
fastapi==0.139.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Optional GenAI provider (app degrades gracefully to offline demo mode without it)
anthropic==0.117.0

# Testing (kept here so `pip install -r requirements.txt && pytest` just works)
pytest==9.1.1
httpx==0.28.1
